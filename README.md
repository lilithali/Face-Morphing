# Face Morphing

Face morphing using Delaunay triangulation, affine warping, bilinear interpolation, and image blending.

## Overview

This project generates smooth transitions between two face images by aligning corresponding facial landmarks and warping both faces into intermediate shapes.

The morphing pipeline uses Delaunay triangulation to divide the face into triangular regions. Each triangle is transformed using affine warping, and bilinear interpolation is applied to obtain smooth pixel values. The warped images are then blended to generate intermediate frames.

## Methods

- Facial landmark correspondence
- Delaunay triangulation
- Affine transformation
- Inverse warping
- Bilinear interpolation
- Image blending

## Technologies

- Python
- NumPy
- SciPy
- Matplotlib
- OpenCV
- Jupyter Notebook

## Results

![Face Morphing Result](images/morph_result.png)

A smooth morphing sequence is generated by interpolating facial landmarks, warping corresponding triangular regions, and blending the warped images.

## Project Structure

```text
face-morphing/
├── README.md
├── requirements.txt
├── face_morphing.ipynb
├── images/
│   └── source_images.png
    ├── triangulation.png
    ├── face_morphing.png
└── demo/
    └── face_morphing.mp4
```

## How to Run

1. Install the required packages:

```bash
pip install -r requirements.txt
```

2. Open the Jupyter notebook:

```bash
jupyter notebook face_morphing.ipynb
```

3. Run the cells in order to generate the intermediate morphing frames and output video.

## Demo

The generated morphing video is available in:

```text
demo/face_morphing.mp4
```
